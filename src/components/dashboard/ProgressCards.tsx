'use client';

interface ProgressCardsProps {
  className?: string;
}

export default function ProgressCards({ className = '' }: ProgressCardsProps) {
  // ProgressCardsコンポーネントは現在使用していません
  // ダッシュボードから統計カードを削除しました
  return null;
}